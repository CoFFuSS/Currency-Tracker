import { StyledContent, StyledFooterContainer, StyledFooterListContainer } from './styled';

export const Footer = (): JSX.Element => (
  <StyledFooterContainer>
    <StyledFooterListContainer>Modsen Currency Tracker</StyledFooterListContainer>
    <StyledContent>
      <p>
        Since then, the company has grown organically to. Starsup is the world's largest trading
        platform, with $12 billion worth of currency trading and 500,000 tickets sold daily to tens
        of thousands of traders worldwide.
      </p>
    </StyledContent>
  </StyledFooterContainer>
);
